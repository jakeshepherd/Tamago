<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jakeshepherd.tamago">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_tamago_round"
        android:label="@string/appName"
        android:roundIcon="@mipmap/ic_launcher_tamago_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditPopup"
            android:theme="@style/AppTheme.CustomTheme" >
        </activity>
        <activity android:name=".Settings" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".Popup"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity
            android:name=".ShoppingListView"
            android:label="@string/title_activity_new_shopping_list_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="Tamago"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".APILink">

        </activity>
        <activity
            android:name=".ManualEntry"
            android:label="Manual Entry" />
        <activity
            android:name=".ShoppingListEntry"
            android:label="ShoppingListEntry" />
        <activity android:name=".ShoppingListDelete" />
    </application>

</manifest>